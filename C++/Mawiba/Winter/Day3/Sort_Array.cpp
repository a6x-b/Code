#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    int Q,N;
    cin >> Q;
    while (Q--)
    {
        bool Lower = 1;
        int N, X;
        cin >> N >> X;
        int arr1[N],arr2[2];
        for (int i = 0; i < N; i++)
            cin >> arr1[i];
        for (int = 0; i<N;i++)
            cin >> arr2[i];
        sort(arr1,arr1+N);
        sort(arr2,arr2+N, greater <int>());
        for (int i = 0; i < N; i++)
            if (arr1 + arr2 >)
    }

    return 0;
}
